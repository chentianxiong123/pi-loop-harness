/**
 * Node property selectors for Cypher queries
 * Re-exports from @core/types to avoid duplication
 */

export {
  EPISODIC_NODE_PROPERTIES,
  STATEMENT_NODE_PROPERTIES,
  ENTITY_NODE_PROPERTIES,
  COMPACTED_SESSION_NODE_PROPERTIES,
} from "@core/types";
