{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-access",
  "description": "Allow remote webapp to use Tauri APIs",
  "remote": {
    "urls": ["http://localhost:3033"]
  },
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
